import {StyleSheet} from 'react-native';

export const STYLES_TABBAR = StyleSheet.create({
  tabBar: {
    height: 70,
    elevation: 4,
    justifyContent: 'center',
    alignItems: 'center',
  },
  label: {
    fontSize: 13,
  },
});
