import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const InforTransferScreen = () => {
  return (
    <View>
      <Text>InforTransferScreen</Text>
    </View>
  );
};

export default InforTransferScreen;

const styles = StyleSheet.create({});
